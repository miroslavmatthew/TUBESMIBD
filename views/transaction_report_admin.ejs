<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='https://fonts.googleapis.com/css?family=Poppins'rel='stylesheet'>
    <link rel="stylesheet" href="asset/transaction_report_admin.css" />
    <script src="https://kit.fontawesome.com/d53ca09937.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/asset/report.js" defer></script>
    <title>Document</title>
</head>
<body>
    <div class="header">
        <img
          src="asset/Group 3.png"
          style="width: 200px"
          onclick="window.location.href='/admin';"
          id="logoJudul"
        />
        <div style="flex-grow: 1"></div>
        <i class="fa-regular fa-user fa-xl"></i>
    </div>
    <main>
        <div>
            <canvas id="myChart" dataMeja="<%= mejaB %>" dataTable="<%= dt %>"></canvas>
          </div>
        <div class="con">
            <div class="search">
                <h2>TRANSACTION REPORT</h2>
                <div class="filter">
                    <select name="status" id="status">
                        <option value="all">All</option> 
                        <% if(member){ %>
                        <option value="member" selected>Member</option> 
                        <% }else{ %>
                        <option value="member">Member</option> 
                        <% } %>
                    </select>
                    <select name="by" id="by">
                        <option value="all">All</option> 
                        <option value="district">District</option> 
                        <option value="subDistrict">Sub District</option> 
                        <option value="urbanVillage">Urban Village</option> 
                    </select>
                </div>
            </div>
            <div class="table">
                <div class="table-header">
                    <div class="header__item">Transaction Date</div>
                    <div class="header__item">No Table</div>
                    <div class="header__item">Sub-total</div>
                </div>
                <div class="table-content">	
                    <%for(let i = 0; i < repo.length; i++){%>
                    <div class="table-row">		
                        <div class="table-data"><%= repo[i].tglTransaksi.toLocaleDateString("en-US") %></div>
                        <div class="table-data"><%= repo[i].noMeja %></div>
                        <div class="table-data"><%= repo[i].hargaTiket %></div>
                    </div>
                    <% }%>
                </div>	
            </div>
            <div class="total">
                <div class="a">
                    <p>Total</p>
                    <% if(repo.length>0){ %>
                        <p id="totalTable" name="totalTable"><%= repo[0].total %></p>
                        <% }else{ %>
                            <p id="totalTable" name="totalTable">0</p>
                        <% } %>
                   
                </div>
                
            </div>
        </div>
    </main>
</body>
</html>